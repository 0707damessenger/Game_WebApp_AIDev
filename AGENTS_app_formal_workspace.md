# 正式应用工作区元规则

本工作区用于将已验证的网页应用原型推进为正式安卓应用或小程序项目。应用工作区采用“单端项目”原则：每个项目只面向一个正式平台；同一产品后续开发另一个平台时，创建新的独立项目。

## 使用者背景

使用者为独立开发者，编程基础和正式应用开发经验较弱。AI 在讨论实现路径时，应优先使用自动化方法；必须手动操作 IDE、真机调试、平台后台或审核流程时，应详细说明操作步骤、原因和预期结果。

## 交互约定

- 遇到需要用户决策的事项，例如目标平台、技术栈、账号配置、权限申请、接口方案、发布方式等，必须优先使用 `AskUserQuestion` 或当前环境等价的用户确认工具提问。
- 不得在用户未确认时自行选择安卓技术栈、小程序平台、云服务、登录方式、支付方式或发布策略。
- 解释实现方案时，应使用清晰的非技术化表述；必要技术细节应说明“为什么需要这样做”。

## 工作区定位

- 本工作区只承载正式安卓应用或正式小程序项目。
- 网页原型只作为产品目的、交互流程和界面结构参考，不作为可直接照搬的工程实现。
- 从原型迁移到正式项目时，必须先阅读原型项目的 `docs/design.md`；若存在探索内容，还应阅读 `docs/exploration.md`，并只迁移已确认内容。

## 开发项目命名方法

每个独立应用项目命名为：

```text
<工作区根>\Project_<X>_<平台>_<项目名>
```

其中 `<平台>` 只能使用当前正式目标平台，例如：

```text
Project_1_安卓_观鸟路线记录
Project_2_小程序_观鸟路线记录
```

- `X` 仅统计 `<工作区根>` 下严格匹配 `Project_<数字>` 的现有文件夹，新项目取其最大编号 +1。
- 起始编号为 1。
- 同一产品的安卓端和小程序端应创建为两个独立项目，不得在同一个项目目录中混合开发。

## 开发项目文件结构

每个正式应用项目必须至少包含：

```text
Project_X_平台_项目名/
├── docs/
│   ├── design.md
│   └── exploration.md
└── app/
```

`app/` 用于存放当前平台的正式工程：

- 安卓项目：`app/` 内为 Android Studio、Gradle、Flutter、React Native 或其他已确认技术栈的工程。
- 小程序项目：`app/` 内为微信小程序或其他已确认小程序平台的工程。

按复杂度可增加：

```text
docs/
├── migration.md     # 原型到正式应用的迁移说明
├── tasks.md         # 当前模块任务、进度和阻塞点
├── test.md          # 测试计划、自测记录、真机验证和兼容性记录
├── release.md       # 版本号、构建产物、提交审核和发布检查
└── api.md           # 接口、数据来源、权限、账号和环境说明
```

扩展文档只在进入对应阶段或项目复杂度需要时创建，不得为了形式提前堆文档。

## 文档职责

### docs/design.md

`docs/design.md` 是正式开发契约，只记录已确认内容：

- 应用核心目的和目标用户
- 页面结构、主要流程、交互规则和状态反馈
- 数据对象的产品含义和用户可见规则
- 已确认的平台目标、权限需求和验收标准
- 核心流程清单：每轮增量开发后必须回归验证的关键用户流程
- 已实现的规则和结构
- 已确认要实现但暂未实现的内容

`design.md` 不写代码级实现细节，例如具体源文件名、函数签名、目录内文件组织、接口参数等。频繁调试的精确数值不写入 `design.md`，除非它们已经成为明确的产品规则或发布验收条件。

### docs/exploration.md

`docs/exploration.md` 只记录未落地内容：

- 备选产品方案和功能候选池
- 未确认的页面、流程、权限、数据结构或商业化方向
- 暂不纳入当前版本的想法
- 已否决的方向：必须附否决理由，防止后续重复提案或无意推翻已定结论
- 需要用户后续判断的取舍

探索内容转为正式结论时，必须先写入 `design.md`，再从 `exploration.md` 移除，避免双份真相。

### 扩展文档

- `migration.md`：记录网页原型中的哪些页面、流程和规则被正式继承，哪些被调整或废弃。
- `tasks.md`：记录当前模块、下一步、阻塞点和已完成节点。
- `test.md`：记录模拟器、真机、小程序开发者工具、兼容性和用户测试反馈。
- `release.md`：记录版本号、构建产物、发布渠道、审核材料、发布前检查和回滚说明。
- `api.md`：记录接口来源、环境、权限、账号依赖、数据字段的产品含义和安全注意事项。

## 开发流程

### 1. 会话开始

- 先检查 Git 状态。
- 执行 `git pull --ff-only` 拉取另一台电脑或远程仓库的最新提交。
- 若存在本地未提交改动导致无法安全同步，必须停止并向用户说明当前状态，不得强制覆盖。
- 同步完成后、动手开发前，必须重读 `docs/design.md` 重建开发契约；若存在 `tasks.md`，一并读取确认当前进度、下一步与阻塞点；涉及探索的还需读 `docs/exploration.md`。未完成上下文重建不得开始开发，以避免跨会话漂移。

### 2. 项目创建或迁移

- 新项目必须先确认项目名称、目标平台、原型来源、技术栈和本次开发范围。
- 安卓技术栈必须逐项确认，不得默认选择原生 Android、Flutter、React Native 或其他方案。
- 小程序平台必须逐项确认，不得默认选择微信、支付宝、抖音或其他平台。
- 若从网页原型迁移，必须先建立或更新正式项目的 `docs/design.md`，明确哪些原型规则进入正式版本。

### 3. 先文档，后开发

以下任一事项发生变化，必须先更新相关文档，再改正式工程：

- 产品目的、页面结构、交互流程、数据规则变化
- 权限需求、接口方案、账号体系、存储策略变化
- 平台目标、技术栈、发布方式变化
- 开始实现此前 `design.md` 尚未确认的模块
- 用户在对话中确认了新的设计或开发结论

更新文档时必须直接在相关章节就地修订，不得重写全文，以保持文档措辞与结构跨轮稳定。

### 4. 增量开发

- 一次只开发一个模块或一个完整用户流程。
- 每个模块必须先有明确验收标准。
- 模块完成后必须进行对应平台的最小可运行验证。
- 发现问题必须立即修复，不得带着未解决问题进入下一个模块。
- 不得一次生成完整系统；模块必须独立实现、独立验证。

### 5. 用户测试

每次完成一个模块、修复一个 bug 或生成一个可体验版本后，必须告知用户：

- 本次完成了什么
- 用户应测试哪个项目或构建
- 建议重点体验哪些行为
- 当前是否存在已知问题

## 平台工程规则

### 安卓项目

- 技术栈必须在项目创建前确认。
- 不得把密钥、证书、接口密钥、平台账号信息提交到仓库。
- 权限申请必须先在 `design.md` 或 `api.md` 中说明用户价值和使用场景。
- 可调配置、环境地址和功能开关应集中管理，不得散落硬编码。
- 生成安装包或测试包前，必须确认版本号、包名、签名方式和目标设备。

### 小程序项目

- 小程序平台必须在项目创建前确认。
- AppID、密钥、后台接口地址等敏感信息不得提交到仓库。
- 平台权限、隐私声明、用户授权弹窗必须先明确用途和触发时机。
- 页面路径、用户流程和数据来源应与 `design.md` 保持一致。
- 提交审核前必须在 `release.md` 中记录版本号、审核说明、测试账号和发布检查项。

## 代码纪律

无论采用何种技术栈，正式工程都必须遵守以下工程约束，以保证多轮开发的一致性与可维护性。

- **不重复造轮子**：新增或修改功能前，必须先检索项目内是否已有可复用的实现（函数、组件、工具、配置）；优先复用与扩展，发现重复实现应合并而非叠加，不得平行造第二套。
- **UI 与逻辑分离**：界面结构与表现属性（颜色、尺寸、间距、文案、主题）必须集中于布局文件、样式表、主题或常量中统一管理，不得散落硬编码进业务逻辑；业务脚本只负责逻辑与数据绑定。
- **界面默认声明式定义**：不在业务逻辑里命令式拼装界面层级；列表等动态内容必须用模板 + 数据驱动，模板本身仍为预定义结构。
- **不写掩盖问题的兜底代码**：禁止静默 `try/catch`、隐藏式默认值、吞掉异常、返回假数据让流程“看起来正常”。区分原则——为掩盖 bug 而容错：禁止；为应对预期内的外部失败（网络失败、权限拒绝、空数据、IO 错误等）而处理：必须，且须给用户明确反馈或留下可排查日志。
- **函数必须加注释**：每个函数/方法必须有注释，至少说明用途；参数、返回值、副作用按需补充。注释解释“做什么/为什么”，不复述代码。
- **配置集中**：可调参数、环境地址、功能开关集中管理（见“平台工程规则”），不得散落为魔法数字。

## 测试与验证

正式应用项目的最小验证包括：

- 工程可安装、可预览或可在开发者工具中运行。
- 当前模块的主流程可完整走通。
- 对照 `design.md` 的核心流程清单逐项回归，确认修改未破坏已确认功能。
- 无新增明显报错、崩溃或阻塞体验的问题。

按复杂度增加：

- 安卓模拟器和真机验证
- 小程序开发者工具预览和真机扫码验证
- 登录、权限、网络异常、空数据、弱网场景验证
- 多屏幕尺寸或系统版本兼容验证
- 发布包或审核包检查

测试结论应写入 `docs/test.md`，或在项目简单时写入 `docs/tasks.md` 的当前模块记录中。

## Git 版本管理

### 远程仓库

- 默认使用 GitHub 私有仓库。
- 新工作区创建后，应在本文件中补充远程仓库地址、本地默认分支和远程跟踪关系。

### 分支使用

- 默认在主干分支开发。
- AI 不得擅自创建、切换或长期保留功能分支。
- 只有用户明确要求分支开发时，AI 才允许创建分支。
- 分支任务完成后，AI 必须提醒用户合并并清理分支。

### 提交决策权

- 提交和推送始终由用户决定。
- AI 不得擅自提交或推送。

AI 在以下节点必须主动询问用户是否提交并推送：

- 确认完某个模块的设计文档
- 完成一个模块并验证通过
- 修复一个 bug 并验证通过
- 生成可体验版本并完成自测
- 更新 `AGENTS.md` 或关键文档
- 本次对话即将结束且存在未提交改动

询问时应简明列出本次改动内容，方便用户判断是否达到提交节点。

### commit message 规范

- 使用简体中文。
- 简短精确描述本次改动内容。
- 示例：`确认路线记录应用核心流程`、`完成记录列表模块并通过验证`、`修复小程序授权失败提示问题`。

## 禁止事项

- 不得擅自覆盖用户改动。
- 不得执行破坏性 Git 操作，例如强制重置、强制切换或清空工作区，除非用户明确要求。
- 不得绕过 `design.md` 直接修改产品规则、页面流程或权限策略。
- 不得擅自选择或更换技术栈。
- 不得提交密钥、证书、账号、Token 或任何敏感配置。
- 不得擅自接入未经确认的 SDK、云服务、支付、广告、统计或登录系统。
- 不得把网页原型代码当作正式应用架构直接照搬。
- 不得平行造重复实现，不得写掩盖问题的兜底代码（详见“代码纪律”）。
