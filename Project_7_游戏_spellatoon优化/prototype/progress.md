# 原型进度

Original prompt: 在不修改项目 6 的前提下新建独立 Spellatoon 优化项目，验证生命压力、中央高权重区域、行动点、每回合两张补牌，并保留局域网与单机双人模式。

## 基础状态与资源经济

- RED：已新增开局资源与回合补给测试，确认因核心模块缺失而失败。
- GREEN：`node --test prototype/tests/rules.test.mjs` 通过。双方起始生命、行动点、手牌和中央高权重格均符合规则；回合开始时行动点恢复受上限限制，补牌受手牌上限限制。

## 加权领地与生命结算

- RED：已新增中央高权重格、落后扣血与生命归零结束对局的测试，确认缺少领地结算 API。
- GREEN：`node --test prototype/tests/rules.test.mjs` 通过。中央高权重格按双倍面积计入；只有领地落后方扣除精确差值生命；生命归零时立即结束对局。

## 自由行动点移动与部署

- RED：已新增可达格与三次混合行动测试，确认移动与部署 API 缺失。
- GREEN：`node --test prototype/tests/rules.test.mjs` 通过。移动与部署均消耗一张牌和一点行动点；同一回合允许任意混合或重复行动，行动点归零后不再接受操作。

## 加权连锁、吞噬与回合结算

- RED：已新增高权重路径计分、结算顺序与回合上限分数决胜测试，确认回合结算 API 缺失。
- GREEN：`node --test prototype/tests/rules.test.mjs` 通过。连锁和吞噬均会为完整路径染色、移除规则指定的卡牌，并按路径的加权格数计分；回合结束先结算领地伤害，若仍在对局中才轮换并恢复行动点、补充手牌，双方达到回合上限后按分数决胜。

## 本地试玩界面

- RED：浏览器契约先确认了界面文件与核心选择器缺失。
- GREEN：本地浏览器契约验证了公共信息、四个中央高权重格、手牌、行动点和结束回合控件；端到端验证了移动、部署与领地伤害结算后的状态同步。

## 局域网房间与私有状态

- RED：已新增私有状态投影与房间授权动作测试，确认投影模块和服务端均不存在。
- GREEN：状态投影、房间创建/加入/开局、授权动作与 SSE 私有同步均已通过测试。公共事件不再携带移动或效果中使用的私有卡牌 ID 与数值。
- GREEN：网页房间入口支持创建、加入与房主开局；服务端向不同设备投影各自手牌，客户端的可达格预览也基于本地私有手牌计算。
- GREEN：双浏览器契约验证了房主创建、玩家加入、房主开局后的手牌隔离，以及行动后双方公共棋盘位置同步。

## 单机双人交接

- RED：已新增开局与换手遮挡手牌的浏览器契约，确认交接幕布缺失。
- GREEN：交接幕布会在开局和换手后隐藏手牌与文本状态中的本地手牌；点击开始行动后才显示。页面提供可见的单机双人入口。

## 整回合生命结算与悬浮预览

- RED：规则测试确认原实现会在先手结束时立即伤害后手；浏览器契约确认界面缺少部署悬浮预览。
- GREEN：双方各自结束一次行动后才结算一次加权领地伤害；部署格悬浮会显示连锁或吞噬路径及加权预计分数，且不改变真实状态。

## 自由预览与锁定

- RED：规则测试确认项目 7 缺少任意空格关联目标、空格锁定和假设部署模拟 API；浏览器契约确认未选牌悬浮时没有关联牌高亮。
- GREEN：未选牌时，悬浮任意格会高亮横纵允许范围内的已部署牌；点击空格锁定后，悬浮高亮牌会以该牌数字模拟连锁或吞噬，并显示高权重计分结果。再次点击锁定格可取消，所有预览均不修改真实棋盘。
- 浏览器检查：锁定格和关联牌具有不同描边，锁定说明与文本状态一致，控制台无错误。
- 后续遗漏模块：回合倒计时与超时处理、局域网客户端断线恢复和房间生命周期、Toast 与游玩指南。
