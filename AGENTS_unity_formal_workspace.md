# 正式 Unity 游戏工作区元规则

本工作区用于将已验证的网页游戏原型推进为正式 Unity 游戏项目。默认引擎版本为 **Unity 2021.2.45f2**。本工作区不用于网页原型试错；网页原型、玩法探索和早期交互验证应在原型工作区完成。

## 使用者背景

使用者为独立开发者，编程基础和 Unity 使用基础较弱。AI 在讨论实现路径时，应优先使用自动化方法；必须手动操作 Unity Editor、打包工具或平台后台时，应详细说明操作步骤、原因和预期结果。

## 交互约定

- 遇到需要用户决策的事项，例如设计取舍、实现方向、平台目标、资源导入、数值锁定、发布方式等，必须优先使用 `AskUserQuestion` 或当前环境等价的用户确认工具提问。
- 不得在用户未确认时自行改变项目方向、引擎版本、目标平台、资源策略或发布策略。
- 解释实现方案时，应使用清晰的非技术化表述；必要技术细节应说明“为什么需要这样做”。

## 工作区定位

- 本工作区只承载正式 Unity 游戏项目。
- 网页原型只作为玩法、交互和视觉节奏参考，不作为可直接照搬的工程实现。
- 从原型迁移到正式项目时，必须先阅读原型项目的 `docs/design.md`；若存在探索内容，还应阅读 `docs/exploration.md`，并只迁移已确认内容。

## 开发项目命名方法

每个独立游戏项目命名为：

```text
<工作区根>\Project_<X>_游戏_<项目名>
```

- `X` 仅统计 `<工作区根>` 下严格匹配 `Project_<数字>` 的现有文件夹，新项目取其最大编号 +1。
- 起始编号为 1。
- `<项目名>` 用于简短传达游戏内容，例如 `Project_1_游戏_观鸟挂机`。

## 开发项目文件结构

每个 Unity 游戏项目必须至少包含：

```text
Project_X_游戏_项目名/
├── docs/
│   ├── design.md
│   └── exploration.md
├── Assets/
├── Packages/
└── ProjectSettings/
```

按复杂度可增加：

```text
docs/
├── migration.md     # 原型到正式项目的迁移说明
├── tasks.md         # 当前模块任务、进度和阻塞点
├── test.md          # 测试计划、自测记录和真机/打包验证记录
├── release.md       # 版本号、构建产物、发布步骤和发布检查
└── assets.md        # 美术、音频、字体、插件等资源来源与授权记录
```

扩展文档只在进入对应阶段或项目复杂度需要时创建，不得为了形式提前堆文档。

## 文档职责

### docs/design.md

`docs/design.md` 是正式开发契约，只记录已确认内容：

- 核心玩法、核心循环、胜负条件、关卡或难度结构
- 玩家输入、交互流程、UI 布局和反馈规则
- 已确认的系统模块边界
- 已确认的资源策略、目标平台和验收标准
- 核心流程清单：每轮增量开发后必须回归验证的关键玩法流程
- 已实现的规则和结构
- 已确认要实现但暂未实现的内容

`design.md` 不写代码级实现细节，例如具体脚本名、函数签名、目录内文件组织、接口参数等。频繁调试的精确数值不写入 `design.md`，除非它们已经成为明确的设计结论或发布验收条件。

### docs/exploration.md

`docs/exploration.md` 只记录未落地内容：

- 备选方案和候选机制
- 未确认的玩法、关卡、系统、资源方向
- 暂不纳入当前版本的想法
- 已否决的方向：必须附否决理由，防止后续重复提案或无意推翻已定结论
- 需要用户后续判断的取舍

探索内容转为正式结论时，必须先写入 `design.md`，再从 `exploration.md` 移除，避免双份真相。

### 扩展文档

- `migration.md`：记录原型来源（工作区、项目名与对应 commit），以及网页原型中的哪些行为被正式继承，哪些被调整或废弃（详见「原型迁移」）。
- `tasks.md`：记录当前模块、下一步、阻塞点和已完成节点。
- `test.md`：记录 Unity Editor 自测、Play Mode 验证、构建验证和用户测试反馈。
- `release.md`：记录正式构建、版本号、发布包路径、发布前检查和回滚说明。
- `assets.md`：记录资源来源、授权、导入位置和替换状态。

## 原型迁移

将原型工作区中已验证的网页原型推进为正式 Unity 项目时，遵循本节流程。迁移的本质是：以原型的 `docs/design.md` 为设计契约、以可运行的原型为行为参照，在 Unity 中重新实现；**不照搬原型代码**。

### 迁移步骤

1. **建正式项目骨架**：按「开发项目命名方法」与「开发项目文件结构」创建项目目录，并用 Unity Hub 以 **2021.2.45f2** 新建空工程。
2. **迁移设计而非代码**：阅读原型 `docs/design.md`（存在探索内容时一并读 `docs/exploration.md`），只取已确认内容，按 Unity 的玩法与交互重写进正式项目的 `docs/design.md`，不复制粘贴。
3. **记录原型来源**：在 `docs/migration.md` 写明原型来源——原型所在工作区、项目名与对应 commit（例如 `Game_WebApp_AIDev / Project_4_观鸟路线记录 @ c36ebec`），以及哪些原型行为被继承、调整或废弃，以便追溯设计依据。
4. **增量重建**：让原型保持可运行作为行为参照，按「增量开发」纪律一次实现并验证一个模块，逐步在 Unity 中重建。

### 不予迁移的内容

- 原型期用于快速试错的精确数值（除非已成为正式设计结论或发布验收条件）
- 原型的工程实现与代码结构（仅作玩法、交互与节奏参照，不作正式架构）

## 开发流程

### 1. 会话开始

- 先检查 Git 状态。
- 执行 `git pull --ff-only` 拉取另一台电脑或远程仓库的最新提交。
- 若存在本地未提交改动导致无法安全同步，必须停止并向用户说明当前状态，不得强制覆盖。
- 同步完成后、动手开发前，必须重读 `docs/design.md` 重建开发契约；若存在 `tasks.md`，一并读取确认当前进度、下一步与阻塞点；涉及探索的还需读 `docs/exploration.md`。未完成上下文重建不得开始开发，以避免跨会话漂移。

### 2. 项目创建或迁移

- 新项目必须先确认项目名称、原型来源、目标平台和本次开发范围。
- Unity 版本固定为 **2021.2.45f2**；不得擅自升级或降级。
- 若从网页原型迁移，必须先建立或更新正式项目的 `docs/design.md`，明确哪些原型规则进入正式版本；完整迁移流程见「原型迁移」一节。

### 3. 先文档，后开发

以下任一事项发生变化，必须先更新相关文档，再改 Unity 项目：

- 玩法规则、胜负条件、交互流程、关卡结构变化
- 系统模块拆分、资源策略、平台目标变化
- 开始实现此前 `design.md` 尚未确认的模块
- 用户在对话中确认了新的设计或开发结论

更新文档时必须直接在相关章节就地修订，不得重写全文，以保持文档措辞与结构跨轮稳定。

### 4. 增量开发

- 一次只开发一个模块。
- 每个模块必须先有明确验收标准。
- 模块完成后必须进行 Unity Editor 内自测；涉及运行体验的模块必须进行 Play Mode 验证。
- 发现问题必须立即修复，不得带着未解决问题进入下一个模块。
- 不得一次生成完整系统；模块必须独立实现、独立验证。

### 5. 用户测试

每次完成一个模块、修复一个 bug 或完成一次可体验构建后，必须告知用户：

- 本次完成了什么
- 用户应测试哪个项目或构建
- 建议重点体验哪些行为
- 当前是否存在已知问题

## Unity 工程规则

- Unity 项目必须能用 Unity 2021.2.45f2 打开。
- `Assets`、`Packages`、`ProjectSettings` 是正式工程的核心目录，必须纳入版本管理。
- 不得提交 Unity 自动生成的本地缓存目录，例如 `Library`、`Temp`、`Obj`、`Logs`。
- 可调参数、功能开关、难度参数应集中在明确的配置来源中，不得散落为魔法数字。
- 资源导入前必须确认来源、用途和授权；不得擅自引入大型插件、付费资源或不可追溯资源。
- 早期正式开发可继续使用占位资源，但占位资源和正式资源的替换关系应在 `assets.md` 或 `tasks.md` 中记录。

## 代码纪律

正式 Unity 工程必须遵守以下工程约束，以保证多轮开发的一致性与可维护性。

- **不重复造轮子**：新增或修改功能前，必须先检索项目内是否已有可复用的实现（脚本、组件、Prefab、工具方法、配置）；优先复用与扩展，发现重复实现应合并而非叠加，不得平行造第二套。
- **UI 与逻辑分离，表现属性外置**：界面与可视节点优先用 Prefab/场景预制；颜色、尺寸、布局、可调数值等表现属性应归于 Inspector 序列化字段、ScriptableObject 或集中配置，不得硬编码进逻辑脚本；脚本只负责逻辑与数据绑定。
- **不在运行时用代码拼装界面层级**：界面结构默认在场景/Prefab 中预制；列表等确需动态生成的内容，必须实例化预定义的 Prefab 并绑定数据，不得在代码里逐节点 `new` 出界面。
- **不写掩盖问题的兜底代码**：禁止静默 `try/catch`、隐藏式默认值、吞掉异常、用假数据让流程“看起来正常”。区分原则——为掩盖 bug 而容错：禁止；为应对预期内的外部失败（资源加载失败、存档缺失或损坏、空引用、IO 错误等）而处理：必须，且须给用户明确反馈或留下可排查日志。
- **函数必须加注释**：每个函数/方法必须有注释，至少说明用途；参数、返回值、副作用按需补充。注释解释“做什么/为什么”，不复述代码。
- **配置集中**：可调参数、功能开关、难度参数集中管理（见“Unity 工程规则”），不得散落为魔法数字。

## 测试与验证

正式游戏项目的最小验证包括：

- Unity Console 无新增编译错误。
- Play Mode 可进入核心流程。
- 当前模块的主要交互可操作、可重复验证。
- 对照 `design.md` 的核心流程清单逐项回归，确认修改未破坏已确认功能。

按复杂度增加：

- 场景切换验证
- 存档/读档验证
- 不同分辨率或输入方式验证
- 目标平台构建验证
- 性能和内存观察
- 用户体验反馈记录

测试结论应写入 `docs/test.md`，或在项目简单时写入 `docs/tasks.md` 的当前模块记录中。

## Git 版本管理

### 远程仓库

- 默认使用 GitHub 私有仓库。
- 新工作区创建后，应在本文件中补充远程仓库地址、本地默认分支和远程跟踪关系。

### 分支使用

- 默认在主干分支开发。
- AI 不得擅自创建、切换或长期保留功能分支。
- 只有用户明确要求分支开发时，AI 才允许创建分支。
- 分支任务完成后，AI 必须提醒用户合并并清理分支。

### 提交决策权

- 提交和推送始终由用户决定。
- AI 不得擅自提交或推送。

AI 在以下节点必须主动询问用户是否提交并推送：

- 确认完某个模块的设计文档
- 完成一个模块并验证通过
- 修复一个 bug 并验证通过
- 生成可体验构建并完成自测
- 更新 `AGENTS.md` 或关键文档
- 本次对话即将结束且存在未提交改动

询问时应简明列出本次改动内容，方便用户判断是否达到提交节点。

### commit message 规范

- 使用简体中文。
- 简短精确描述本次改动内容。
- 示例：`确认正式版核心循环设计`、`完成角色移动模块并通过验证`、`修复场景切换后输入失效问题`。

## 禁止事项

- 不得擅自覆盖用户改动。
- 不得执行破坏性 Git 操作，例如强制重置、强制切换或清空工作区，除非用户明确要求。
- 不得绕过 `design.md` 直接修改玩法、规则或交互。
- 不得擅自升级 Unity 版本。
- 不得引入未经确认的插件、SDK、付费资源或外部服务。
- 不得把原型阶段的临时代码当作正式工程架构直接照搬。
- 不得平行造重复实现，不得写掩盖问题的兜底代码（详见“代码纪律”）。
