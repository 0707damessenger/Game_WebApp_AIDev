Original prompt: 请继续开发项目4

# Project 4 Progress

## 2026-06-11

- 接手后已执行 `git pull`，远端为最新。
- 当前设计文档确认阶段范围：核心记录闭环、个人入口、历史记录列表；后续模块暂不实现。
- 当前优先来自 `docs/temporary_todo.md`：真机 GPS 验证。
- 发现待修细节：GPS 模式下如果地图瓦片失败并启用测试兜底，选起点状态仍会显示“使用测试起点”按钮，但 GPS 设计要求定位失败时停留在选起点状态、不生成虚假轨迹。
- 已修复：模拟兜底起点只在模拟行走模式暴露；GPS 待定位状态保持禁用的“添加鸟种”按钮。
- 已修复：瓦片加载失败提示条改为不接收点击，避免覆盖开始按钮等控件。
- 验证记录：`npm test` 通过；Playwright 视觉脚本完成模拟记录闭环、结果页、历史列表与 GPS 待定位状态截图检查。
- 真机 GPS 已由用户确认功能正常；`locationSource` 已回退为 `simulated`。
- 后续 TODO：继续开发后续模块前，需先确认下一个模块的设计范围。候选方向为国内稳定地图源切换、鸟类名录扩展、分享与导入、个人数据模块。

## 2026-06-12

- 已确认并进入“鸟类名录扩展”模块：全国正式鸟类记录，本次只支持中文名与学名搜索。
- 已修复根目录测试脚本在当前 Node 版本下把测试目录当作模块入口的问题。
- 已新增名录资源测试，覆盖名录规模、代表性鸟种、字段完整性与重复项。
- 已将本地 `BIRD_CATALOG` 扩展为 1501 条正式物种记录，并保留原有 `name` / `scientificName` 数据接口。
- 已补充 `docs/catalog_source.md`，记录当前数据来源、处理边界和后续授权注意点。
- 已新增端到端测试，覆盖中文名搜索、学名搜索、无结果状态、选择鸟种并添加到当前位置。
- 已完成手机视口截图目检：添加鸟种弹层在大名录搜索下无明显溢出或重叠；地图瓦片网络超时仍会出现，但不影响本地名录搜索。
- 后续 TODO：请用户体验验证常用鸟名搜索结果是否符合预期；若后续需要拼音首字母、别名、英文名或附录条目标记，应作为独立模块先确认范围。
- 修复路线重复显示问题：地图瓦片失败启用背景点击兜底时，不再同时绘制测试 SVG 路线与 Leaflet 路线。
- 验证记录：新增端到端回归测试覆盖瓦片失败后实际点击地图的场景；`npm.cmd test` 通过；手机视口截图确认只显示一条路线。
- 已进入并完成“国内稳定地图源切换”模块：设计文档将天地图国内瓦片源纳入当前阶段，探索文档移除地图源候选。
- 已将地图配置调整为默认天地图矢量底图 + 中文注记，并保留 OpenStreetMap 作为公共回退源；当前天地图 Key 使用占位值，等待用户提供真实 Key 后验证真实瓦片加载。
- 已新增配置与地图初始化静态测试，覆盖唯一 `CONFIG`、天地图默认源、OpenStreetMap 回退源与按 provider 加载瓦片层。
- 已新增端到端测试，覆盖默认请求天地图瓦片、天地图瓦片失败时仍可继续流程、且不重复绘制路线。
- 验证记录：`npm.cmd test` 通过；内置浏览器手机视口打开本地预览，确认首屏、开始记录、瓦片失败提示、兜底点击进入记录中与路线层显示正常。

## 2026-06-12 历史记录编辑·删除·落点拖动

- 已进入并完成「历史记录编辑·删除·落点拖动」模块：历史记录查看页可进入编辑模式，支持保存与取消。
- 编辑模式下可编辑鸟种落点信息、删除单个鸟种落点，并可拖动落点吸附到已记录轨迹上。
- 历史列表支持删除整条记录，删除前需要二次确认。
- 已修复瓦片失败时落点定位、拖动判定和地图平移跟随的问题，确保真实地图对象存在时不退回伪坐标兜底。
- 验证记录：端到端测试覆盖历史编辑持久化、取消还原、落点拖动、瓦片失败下拖动与整条历史删除。

## 2026-06-12 分享与导入交互骨架

- 已进入并完成「分享与导入交互骨架」模块：结果页与历史记录详情页均可打开分享弹窗；个人页新增导入分享链接入口。
- 分享弹窗展示记录摘要、完整路线与鸟点位置敏感提示，并明确服务器链接服务待接入；当前不生成真实短链接、图片或二维码。
- 导入弹窗支持粘贴分享链接并展示待接入预览，占位状态下不会写入历史记录。
- 已新增配置与状态层测试，覆盖分享导入服务默认关闭、重复导入未来按「打开已有记录」策略处理。
- 验证记录：`npm test` 通过；内置浏览器手机视口检查分享弹窗、历史详情分享、导入弹窗与按钮文字显示正常。
- 后续 TODO：真实分享服务接入前，需先确认服务器短链接、链接失效/权限、真实导入校验与图片或二维码分享是否同模块推进。

## 2026-06-14 本地收藏线路

- 已进入并完成「本地收藏线路」模块：个人页新增收藏线路入口，收藏列表展示本机历史记录中已收藏的路线。
- 历史记录列表与历史详情页均支持收藏 / 取消收藏；旧历史记录没有收藏状态时按未收藏处理。
- 删除历史记录时不会保留独立收藏副本；收藏状态随同一条历史记录一并删除。
- 已新增状态层测试，覆盖旧记录默认未收藏、收藏切换、收藏列表筛选与删除后的收藏结果。
- 已新增端到端测试，覆盖从历史列表收藏、从收藏列表打开、在详情页取消收藏以及空收藏列表状态。
- 验证记录：`npm test` 通过；手机视口截图检查个人页、收藏列表与历史详情按钮无重叠或横向溢出。
- 注意：会话开始按规则尝试 `git pull`，但当前机器无法连接 GitHub 443 端口，未能完成远端同步。


## 2026-06-18 设置模块

- 已进入并完成「设置」模块：个人页原「设置（后续模块）」占位按钮改为可用入口，进入独立全屏设置页。
- 设置页提供三项功能：清空本地数据（一键清空历史/收藏/未保存草稿，二次确认、不可撤销）、版本信息（只读展示原型版本号与名录/特征/地图数据来源说明）、定位来源切换（真实 GPS / 模拟行走，写回唯一 CONFIG，下次开始记录生效）。
- 设计文档先行更新：design.md 第 1 节阶段范围加入「设置」，新增 4.8 设置页、第 5 节设置模块，并从第 7 节后续模块移除设置基础项。
- 版本号由唯一配置源 CONFIG.appVersion 承载（当前 0.8.0）。
- 验证记录：单元测试 45 项全部通过（含新增设置资源测试）；新增 2 项设置端到端测试（版本展示+定位切换、清空数据取消/确认）均通过；手机视口下设置页布局无溢出。
- 发现一处既有失败（非本模块引入）：端到端「鸟种模糊匹配」用例在清空工作区后仍失败，疑似上一批 4 维特征数据调整后「大天鹅」候选排序或阈值与用例预期不符。已向用户报告，待确认是否作为独立修复推进。


## 2026-06-18 修复模糊匹配端到端用例

- 排查既有失败用例「鸟种模糊匹配」：根因是上一批「特征改为 4 维 + 接入真实特征数据」提交后，真实数据中大天鹅体型为「很大(veryLarge)」，而该用例仍按「大型(large)」选择特征，导致大天鹅丢失体型权重、跌出候选前列。生产匹配逻辑本身正确，属用例数据过期。
- 修复方式（仅改测试）：用例改按大天鹅真实特征选择「很大 / 游水 / 湿地 + 河流」，使其成为唯一最高分候选（9 分，次高 7 分）。
- 验证记录：单元测试 45 项、端到端 21 项全部通过。
