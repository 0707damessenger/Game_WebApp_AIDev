# 工作区元规则

本工作区用于设计开发独立游戏网页原型与网页端应用程序。网页原型后续可能转为 Unity 项目（引擎版本 2021.2.45f2），由其他工作区负责。

## 使用者背景

使用者为独立开发者，编程基础和引擎使用基础薄弱。在讨论实现路径时，应优先使用自动化方法；需要手动操作的步骤，须尽量详细地说明操作方法和原理。

## 交互约定

- 遇到需要用户决策的事项（如设计取舍、实现方向、参数选择等），必须优先使用当前环境可用的选择式提问工具（如 `AskUserQuestion`、`request_user_input` 或同等功能工具）向用户明确提问，不得自行猜测或默认选择。若当前环境没有可用的选择式提问工具，必须用简短纯文本问题逐项询问，并说明工具不可用原因。
- 涉及界面布局、视觉方案、交互流程图或其他视觉内容时，默认允许使用可视化伴侣，无需再次征询；仅当用户明确表示不使用时才停用。

## 项目结构与归属

每个项目命名为 `<工作区根>\Project_<X>_项目名`；`X` 只统计根目录下严格匹配 `Project_<数字>_` 的项目目录，新项目取最大编号 +1，起始编号为 1。

每个项目必须包含：

```
Project_X_项目名/
├── docs/
└── prototype/
```

项目专属的文档、过程稿、配置、脚本、测试和产物都必须放入对应项目。工作区根目录只放服务于多个项目的配置、约束、依赖或工具。

根目录禁止创建或保留：`docs/`、`output/`、`test-results/`、`tests/`、`playwright-report/`。其中：文档放项目 `docs/`，测试放项目 `prototype/tests/`，截图和导出放项目 `output/`，Playwright 失败产物放项目 `prototype/test-results/`，日志和临时文件放项目内专用目录。

开始任务前先确认目标项目并查看 `git status`；保留用户已有改动，不得覆盖、重置、删除或回退无关内容。运行服务、测试或验证工具时必须明确项目目录和产物目录，禁止让相对路径默认落到工作区根目录。发现根目录违规目录时，先归档有价值内容、清理临时产物并修正入口，再继续工作。

## 设计文档与探索

- 开始实现一个项目或新增设计模块前，先与用户确认方向，再创建或更新 `<项目目录>\docs\design.md`。它是该项目的设计契约，只记录已实现或已确认的规则、结构和交互，不记录源文件、函数接口等代码实现细节。
- 玩法规则、胜负条件、数值逻辑、关卡结构、交互流程、模块边界或资源策略发生设计变化时，必须先更新 `design.md`，再改代码。普通 Bug 修复、重构和不改变设计的工具维护无需修改设计文档；文档应直接修改相关章节，不重写全文。
- 备选方案、未收敛的脑暴、正在探索的机制和暂不纳入的方向放入项目 `docs/exploration.md` 或已约定的独立探索文档。探索内容落地时先写入 `design.md`，再从探索文档移除，避免双份真相。
- 不在设计文档中记录可调配置参数；固定规则所必需的数量、上限或范围可以记录。

## 增量开发与验证

- 一次只推进一个可独立验证的模块；当前增量的问题必须在进入下一增量前解决。
- 网页原型或界面改动必须在浏览器中验证；纯文档、工具或非界面逻辑改动按实际风险选择测试方式。
- 每个增量都要有对应的自动化或人工验证；验证通过后告知用户需要体验的项目，并再推进下一模块。
- 输出必须是可运行的网页原型，不一次生成未经验证的完整系统。

## 配置与资源策略

### 配置

网页原型阶段，每个项目的全部属性数值与功能开关必须集中在该项目唯一的 `CONFIG` 对象中，修改后直接刷新即可验证。`CONFIG` 的承载方式二选一，由项目复杂度决定：

- **内联**：写在 HTML 文件顶部的 `<script>` 中，适合单文件小型原型。
- **独立文件**：写在 `prototype/js/config.js` 中，并由 HTML 的首个 `<script>` 引入，适合多文件原型。

无论采用哪种方式，每个项目只能有一个 `CONFIG` 对象作为唯一配置源；不得另起并行配置对象，也不得把数值散落为各处硬编码。

### 资源

网页原型阶段默认使用纯色几何图形占位；只有用户提供、确认或项目已有的资源才可使用，不擅自引入新的美术资源。

## Git 版本管理

默认在当前主分支工作；AI 不得擅自创建或切换分支，除非用户明确要求。

会话开始执行 `git pull --ff-only`。若因网络或远程分叉失败，不得强制覆盖本地改动；记录失败原因并在继续操作前确认本地与远程状态。

AI 不得擅自提交或推送。完成一个独立可验证目标，或本次会话结束仍有未提交改动时，应列出改动并询问用户是否提交。每次提交只包含一个逻辑单元，提交信息使用简体中文、简短准确。
