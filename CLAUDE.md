# 工作区元规则

本工作区用于设计开发独立游戏网页原型与网页端应用程序。网页原型后续可能转为 Unity 项目（引擎版本 2021.2.45f2），由其他工作区负责。

## 使用者背景

使用者为独立游戏开发者，编程基础和引擎使用基础薄弱。在讨论实现路径时，应优先使用自动化方法；需要手动操作的步骤，须尽量详细地说明操作方法和原理。

## 交互约定

- 遇到需要用户决策的事项（如设计取舍、实现方向、参数选择等），必须优先使用 `AskUserQuestion` 工具向用户明确提问，不得自行猜测或默认选择。

## 开发项目命名方法

每个独立项目命名为 `<工作区根>\Project_<X>_项目名`。

- `X` 仅统计 `<工作区根>` 下严格匹配 `Project_<数字>` 的现有文件夹，新项目取其最大编号 +1。
- 起始编号为 1。
- 项目名用于区分项目类型和简单传达项目内容，如 `<网页程序_mbti测试>`、`<游戏_祖玛>`。

业务子目录可在项目目录下按需创建。

## 开发项目文件结构

每个项目必须包含：

```
Project_X_项目名/
├── docs/           # 文档文件夹
└── prototype/      # 网页原型文件夹
```

工作区内其他文件视为过程稿或用于辅助各项目开发的内容。

## 开发流程

### 1. 生成设计文档

- 必须先与用户逐项确认，再创建或更新 `docs\design.md`（核心玩法、玩法循环、规则、模块拆分、关卡/难度、UI 布局、交互逻辑等）。
- `docs\design.md` 是开发契约。后续工作以此文档为准；设计变更必须先改文档再改原型。

### 2. 网页原型

- 必须在浏览器中自我验证并持续迭代，直至手感与规则确认无误。
- 输出：可运行的网页原型。

## 增量开发原则

- 一次只开发一个模块，验证通过后再推进下一模块。
- 发现问题必须立即修复，不得带着未解决的问题进入下一模块。
- 不得一次生成完整系统；每个模块必须独立提交、独立验证。

## 内容纪律

### design.md 的内容边界

`docs\design.md` 只能包含两类内容：

- 已实现的规则、结构
- 已确认要实现但暂未实现的内容（方向已锁定、范围已定）

### 必须单独存放的内容

以下内容必须单独存放于 `docs\` 下独立文档（默认 `docs\exploration.md`，skills 可在 SKILL.md 中另行声明专属探索文档路径，声明格式待定）：

- 备选方案与候选池
- 未收敛的脑暴过程、发散目标、筛选维度
- 正在探索但未确认落地的机制、地图或规则
- 已明确暂不纳入的方向

### 数据精度限制

为避免调试玩法过程中的配置修改导致多次更改文档，design 文档中**不可记录精准数值**。

### 双份真相禁止

- 同一主题同时出现在 `design.md` 与探索文档时，以 `design.md` 为准。
- 探索转落地时，必须先写入 `design.md`，再从探索文档移除，避免双份真相。

## 文档维护规范

`docs\design.md` 是开发流程的强制环节，不是收尾可选项。

任何设计结论或开发约定变更都必须 **先改文档，再改代码/原型/配置/资源**。直接在相关章节就地修订，**不得重写全文。**

### 触发场景

出现以下任一项，继续前必须先更新文档：

- 玩法规则、胜负条件、数值逻辑、关卡结构、交互流程变化
- 模块拆分、文件结构、接口约定、配置结构、资源策略变化
- 开始实现此前文档中尚未明确约定的模块
- 用户在对话中确认了新的设计结论或开发结论

## 配置与资源策略

### 配置

网页原型阶段的全部属性数值与功能开关必须集中在唯一的全局 `CONFIG` 对象中，修改后直接刷新即可验证。`CONFIG` 的承载方式二选一，由项目复杂度决定：

- **内联**：写在 HTML 文件顶部的 `<script>` 中，适合单文件小型原型。
- **独立文件**：写在 `prototype/js/config.js` 中，并由 HTML 的首个 `<script>` 引入，适合多文件原型。

无论采用哪种方式，全项目只能有一个 `CONFIG` 对象作为唯一配置源；不得另起并行配置对象，也不得把数值散落为各处硬编码。

### 资源

网页原型阶段必须使用纯色几何图形占位，不依赖美术资源。

## Git 版本管理

### 远程仓库

- 平台：GitHub（私有仓库）
- 地址：`https://github.com/0707damessenger/Game_WebApp_AIDev.git`
- 本地分支：`master`，已关联远程 `origin/master`

### 多电脑同步场景

使用者在家和公司两台电脑之间切换开发。同步策略：

- **会话开始**：AI 应先执行 `git pull` 拉取另一台电脑的最新提交，避免冲突
- **提交决策权**：提交始终由用户决定，AI 不得擅自提交或推送
- **会话结束前**：AI 应主动询问用户是否需要提交并推送当前改动

### 提交流程

AI 在以下节点**必须使用 `AskUserQuestion` 主动询问**用户是否提交+推送：

- 完成一个模块的原型开发并验证通过
- 修复一个 bug 并验证通过
- 确认完某个模块的设计文档
- 更新 CLAUDE.md 或设计文档
- 本次对话即将结束且存在未提交的改动

询问时应简明列出本次改动内容，让用户判断是否达到提交节点。

### 提交粒度

每次提交应为一个逻辑单元，对应增量开发中一个模块的完成节点：

- 确认完某个模块的设计文档
- 完成一个模块的原型开发并验证通过
- 修复一个 bug
- 更新 CLAUDE.md 或设计文档

### commit message 规范

- 使用简体中文
- 简短精确描述本次改动内容
- 示例：`完成球体发射模块的原型开发与验证`、`修复碰撞检测边界判定错误`
