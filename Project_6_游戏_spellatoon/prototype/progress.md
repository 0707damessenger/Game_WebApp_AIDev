Original prompt: 负责 Spellatoon 实现计划的 Task 1：基础棋盘、角色、数字卡牌状态。实现 6x6 棋盘、两个对角角色、数字卡牌初始手牌和最小可运行浏览器界面；使用原生 HTML/CSS/ES modules；提供 render_game_to_text() 与 advanceTime(ms)；先写失败测试再实现；不要提交或推送。

## Task 1 进度

- [x] 读取工作区 AGENTS.md 与测试驱动开发规范。
- [x] 按 TDD 先创建初始状态测试并运行，确认目标模块缺失导致失败。
- [x] 建立唯一 CONFIG 对象、初始状态规则 API 与初始状态测试。
- [x] 完成浏览器界面并进行截图、文本和控制台验证。

## 验证记录

- 红灯：`node --test Project_6_游戏_spellatoon/prototype/tests/rules.test.mjs`，因 `config.mjs` 不存在失败。
- 绿灯：`npm run test:spellatoon`，5/5 通过；覆盖棋盘、角色、开局手牌、动作状态、私有手牌文本和未染色格角色定位。
- 浏览器：`python -m http.server 51359 --directory "Project_6_游戏_spellatoon/prototype"` 配合官方 Playwright 客户端验证通过；截图显示 Spellatoon 棋盘和左上角角色，文本状态显示 36 个公共格、双方角色、5 张本地手牌且无 opponentHand，控制台错误为空。
- 修复：角色不再依赖地块染色归属；卡片使用独立节点；动作标志统一为 `moved/deployed`；卡片包含 `ownerId`；玩家 id 统一为 `p1/p2`。

## Task 2 进度

- [x] 增加移动与部署规则 API：数字决定最大移动格数，路径支持横纵任意转弯；部署只能发生在角色所在空格。
- [x] 增加手牌选择、移动/部署模式、逐格路径选择、确认移动和部署点击交互。
- [x] 增加可达格、移动路径、部署目标、选中手牌和无效操作反馈。
- [x] 增加浏览器契约测试：移动、部署和无效移动反馈。

## Task 2 验证记录

- 规则测试与浏览器契约测试：14/14 通过；覆盖移动、部署和无效移动反馈。
- 官方网页游戏 Playwright 客户端：完成页面动作循环，输出截图与状态快照；状态显示 p1 先手、6×6 棋盘、5 张本地手牌；无控制台错误。

## Task 3 进度

- [x] 增加同色连锁与异色吞噬的路径判定，效果仅检查本次新部署卡牌所在的横线与竖线。
- [x] 按路径长度和卡牌数字分别计分，支持横纵交叉、路径卡牌移除和中间非参与卡牌保留。
- [x] 部署事件公开效果类型、方向、完整路径、移除卡牌和得分变化；效果不递归触发。

## Task 3 验证记录

- 规则测试与浏览器契约测试：19/19 通过；覆盖同色连锁、异色吞噬、四格空隙边界、横纵交叉、路径卡牌移除和不递归触发。
- 官方网页游戏 Playwright 客户端：完成页面动作循环，输出截图与状态快照；事件栏、分数和棋盘渲染无回归；无控制台错误。

## Task 4 进度

- [x] 增加硬币先手、回合开始补牌、手牌上限截断和行动标记重置。
- [x] 增加结束回合、双方交替行动、每名玩家独立完成回合数和最终胜负/平局结果。
- [x] 控制台增加结束回合按钮、回合进度、等待状态和结算结果显示。

## Task 4 验证记录

- 规则测试与浏览器契约测试：26/26 通过；覆盖两种先手、补牌上限、等待玩家拒绝结束、行动顺序、回合切换、回合上限、胜负和平局。
- 官方网页游戏 Playwright 客户端：完成结束回合动作循环，输出截图与状态快照；显示 p2 行动、p1 完成 1/10 回合，按钮禁用状态正确；无控制台错误。

## Task 5 进度

- [x] 增加横纵范围内卡牌目标高亮、空格锁定和卡牌悬停效果预览。
- [x] 预览使用假设数字复用连锁/吞噬解析，显示路径、效果类型和预计得分，不改变真实棋盘、手牌、分数或行动次数。
- [x] 等待对手期间保持预览可用；卡牌格点击只提示不能锁定。

## Task 5 验证记录

- 规则测试与浏览器契约测试：31/31 通过；覆盖横纵目标、范围、空格锁定、连锁/吞噬模拟、等待状态和真实状态不变。
- 官方网页游戏 Playwright 客户端：完成页面动作循环，输出截图与状态快照；预览提示、棋盘和回合控制台布局正常；无控制台错误。
- 待办：进入两台设备局域网房间、私有手牌投影和实时同步模块。

## Task 6 进度

- [x] 增加局域网房间服务：创建房间、加入房间、房主开始对局和断线重连。
- [x] 增加按玩家隔离的公共状态投影与 SSE 实时同步；对手手牌不会发送到另一台设备。
- [x] 联机客户端接入创建、加入、开始、移动、部署和结束回合流程。

## Task 6 验证记录

- 单元、服务器和浏览器契约测试：37/37 通过。
- 真实服务双页面验证：房主创建房间，第二页面加入，房主开始；红方移动后蓝方实时收到位置更新，蓝方部署后红方实时看到公共卡牌，等待状态与行动按钮正确切换。
- 手牌隔离验证：两端只显示本地手牌，公共状态和实时更新中不包含对手手牌。
- 官方网页游戏 Playwright 客户端：真实服务页面截图、状态快照生成成功；截图确认本地手牌、双方公开角色信息与行动控制台可见；无控制台错误。
- 当前服务地址：`http://127.0.0.1:51359/`。

## Task 7 进度

- [x] 按确认的交互方案完成 PC 与手机响应式布局，控制台仅保留当前玩家手牌、结束回合和必要提示。
- [x] 选牌后自动显示移动目标与部署位置；目标第一次点击进入待确认，第二次点击提交；支持自动最短路径、切换目标、切换手牌和 Esc 取消。
- [x] 修复联机公开状态不包含玩家手牌导致选牌后可达格计算报错的问题；规则计算会使用本机私有手牌，不向界面或网络暴露对手手牌。
- [x] 修复重复 SSE 状态推送清空本地选择的问题；视图未发生变化时保留选牌和待确认状态。

## Task 7 验证记录

- 联机浏览器回归测试：1/1 通过，覆盖选牌、移动目标二次确认、主机提交和对手实时收到位置更新。
- 全量 Spellatoon 测试：43/43 通过，覆盖规则、公开状态、房间服务、布局、交互和预览。
- [x] 官方 Playwright 客户端复核真实页面截图、文本状态和控制台错误；选牌后的高亮、部署提示和移动提示均可见，无控制台错误。
